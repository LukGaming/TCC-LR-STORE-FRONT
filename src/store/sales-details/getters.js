export const getters = {
  isLoading(state) {
    return state.isLoading;
  },
  sale(state) {
    return state.sale;
  },
  debits(state) {
    return state.debits;
  },
  credits(state) {
    return state.credits;
  },
  installments_quantity(state) {
    return state.installments_quantity;
  },
  installments_percentage(state) {
    return state.installments_percentage;
  },
};
